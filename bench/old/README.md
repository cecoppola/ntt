Superseded programs, kept buildable (`make old`) for the record:
`04_ntt_lds` and `05_ntt_reg` → `06_ntt_opt`; `08_multiply` → `13_multiply2`.
They carry their own copies of the primitives that now live in `../ntt_kernels.h`.
