You will write a set of unit and function sets that cover everything that this codebase does. 

You will run these tests after every major addition or revision to the code, and ensure that functionality is not lost during development.

You will construct a set of benchmarks, and over time build a table of performance measurements to help optimize the code and prepare it for deployment in the target hardware environment.