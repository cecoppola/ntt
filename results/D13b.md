# D13b — the models and the design-table generator (Phase 13b, PLAN.md §31 row D)

Branch `p13b-D` (from `p13b-step0` @ c988218). Files: `ecalc/mem_model.py`, `ecalc/mn_model.py`, `ecalc/estimate.py`,
new `ecalc/design_table.py`, `docs/TARGET.md` (§1, §3, §6), new `results/D13b.md`, `results/DESIGN_TABLE.md`, and
`results/D13b_strategy_e0.txt` (agent B's `t_strategy` lines, copied as a model input). No C touched. No node used; the
aac6 logs were read only.

Labels: **measured** = an aac6 run; **modelled** = arithmetic on measured inputs; **fitted** = a constant fitted to
measured runs (named with its runs); **assumed** = a target parameter.

## Headline

- `./design_table.py` writes the 96-row table (`results/DESIGN_TABLE.md`, 2 min on the login node). The table marks the
  Pareto front, the fastest row, the largest row and a recommended balance, and checks the ranking at 50 / 100 / 200 GB/s
  per APU. `--mrun <log>` takes the integrator's measurements; `--calibrate` prints model against measured; `--make-line`
  turns an ecalc log into an M-run line.
- **Calibration gate: PASS.** Every size-1 series from 10¹⁰ to 10¹¹ that the model covers is within 3 % (worst
  −1.8 %, P's 3·2²⁹ run; the three-prime series 68.3 s against 67.5 modelled, −0.6 %). Every device total from 10⁹ to
  10¹¹ is within 0.05 %. Every node peak from 4 × 10¹⁰ up is within 0.14 %. The exceptions are listed below with reasons.
- **The current table (all modelled; 100 GB/s per APU assumed)**, the marked rows:

| row | 576-node digits at 502 / 480 GB | wall at 4 × 10¹³ (50 / 100 / 200 GB/s) | wall at the 502-GB max | one node, 4 × 10¹⁰ |
|---|---|---|---|---|
| step 0 alone (C, cap rule = 2³¹ at 576, off, depth 1) | 4.20 / 3.94 × 10¹³ | 4.41 / **3.74** / 3.41 min | 3.86 min | 67.1 s (cap 3·2³⁰ by the rule), 300 GB |
| **F** fastest: C, 2³¹, off, depth 2 | 4.16 / 3.90 × 10¹³ | 4.30 / **3.63** / 3.30 min | 3.70 min | 67.6 s, 248 GB |
| **R** recommended: C, 2³¹, both chunkings, depth 2 | **5.52 / 5.19 × 10¹³** | 4.54 / **3.87** / 3.54 min | 6.22 min | 67.6 s, 248 GB |
| **L** largest: B4, 2³⁰, both chunkings, depth 1 | **6.40 / 6.04 × 10¹³** | 6.71 / 5.43 / 4.86 min | 12.1 min | 78.4 s, 197 GB |

  The environment of R is `ECALC_PLANE_CAP=2^31 MDB_SHIFT_CHUNK_MB=1024 MN_T_CHUNK_MB=1024 COMM_ALLTOALLV_DEPTH=2`, on
  step 0's defaults. The full 96 rows are in `results/DESIGN_TABLE.md`.
- **What the table says** (modelled; it is conclusive only after the M-run replaces the per-node inputs):
  1. **The product strategy hardly matters at 576.** RNS_STRATEGY acts on dist_core, which at 576 means each node's own
     top tree levels. B and B4 forced save 2–3 s there, but pay the same again mapping their extra planes. B at 2³¹ takes
     32 GiB more on each of three APUs, which cuts the ceiling to 2.9 × 10¹³. `auto` equals C at the cap, because a
     capped piece's plane does not fit a pool (b_choose), and it never allocates. At size 1 the strategy does matter:
     B / B4 give 66.1–66.2 s at 2³¹ against 67.6 s for C.
  2. **The 2³¹ cap is the best cap at 576.** The 3·2³⁰ planes cost 52 GB per node at 576 for no gain, because the mn
     tier's pieces follow `POOL_LOG` and not the radix-3 planes. The POOL_LOG-30 caps (2³⁰, 3·2²⁹) buy memory
     (+0.4–0.6 × 10¹³ digits) at +1.3–1.4 min of wall. They are the only way to 6 × 10¹³.
  3. **Chunking pays.** `MDB_SHIFT_CHUNK_MB` alone gives +0.8 × 10¹³ digits for +0.05 min. Both switches give
     +1.4 × 10¹³ for +0.24 min at T_ROUND = 0.03 s per round. The cost of a round on the target is the largest
     unmeasured input for the chunked rows: at 0.01 s the recommended row takes 3.70 min, at 0.1 s it takes 4.47 min.
  4. **Depth 2 is worth 0.11 min at 4 × 10¹³ and costs 0.09 × 10¹³ of ceiling** (one more slab pair per APU). This is
     modelled: the general map at depth 2 hides the same 3/4 as the equal path. X13 measured 1.1 % at depth 1.
  5. **The ranking does not depend on the fabric.** The Spearman correlation of the 4 × 10¹³ walls is 0.999 between 50
     and 100 GB/s and 1.000 between 200 and 100 GB/s, and F is the fastest row at all three.

## 1. What the models now take (the design)

`mn_model.Design(np, strategy, cap, chunk, depth, modmul)`: the code after step 0 is `Design()` (three primes, C, the
code's cap rule, no chunking, depth 1, `NTT_MODMUL=1`). The legacy path, `design=None`, keeps the pre-13b constants:
four primes and the Phase 10/11 phase table. `--calib` and `estimate.py --legacy` use it and reproduce the Phase 12
numbers (6.95 × 10¹⁰ per node, 4.3 min).

| input | how the model uses it | source | label |
|---|---|---|---|
| prime count `ECALC_NP` | pool 0 = np·q, pool 1 = 3q + 16 whatever np (P3's finding; P13b's `rns_plane_pool_bytes`, the same formula); np transforms per piece in the mn tier; per-phase factors from the 4 × 10¹⁰ series | P3, RESULTS §78 (nine NP = 3 runs) | measured / fitted |
| strategy C / B / B4 / auto | the per-product law of the pipeline's big products (a Python port of `t_cap`: the tree's two top levels, the reciprocal's last three doublings, the division's two products, the grid `split_grid_cap` forms, the cuts) from `t_strategy` medians; B / B4 memory = the pools + `b_place`'s grow-only extra for the cap's plane; auto = the B form (B4 at P = 3) only where every plane fits a pool | S13 E0 (`results/S13_e0.txt`), agent B's library forms LC / LB / LB4 and B4 (`results/D13b_strategy_e0.txt`, job 21047); `rns_dist.c` b_choose / b_place / b_planes ported | measured (2ᵏ planes), modelled (3·2ᵏ: 1.575 × the 2ᵏ⁺¹ time) |
| plane cap 2³⁰ / 3·2²⁹ / 2³¹ / 3·2³⁰ | `ECALC_PLANE_CAP` → POOL_LOG + the 3·2ᵏ planes: the plane pools, the dm layout's piece, the mn tier's cap (`mn_cap_log` at POOL_LOG); the batch tier per cap; at POOL_LOG 30 the big products × `cap_factor` (1.39 at 2³⁰, 1.30 at 3·2²⁹) | P13b's runs (job 21046: 4 × 10¹⁰ at 2³⁰ 84.2 s, 3·2²⁹ 76.8 s); I's 2³¹ runs at four primes | measured / fitted |
| chunking off / shift / both | `mem_model` exchange and T scratch (M13's port, exact); time: each extra round = a small all-to-all's latency + a collective + `T_ROUND` | M13 (1e10/4, 1e10/2 at 64 MB: T_ROUND 0.03 s, least squares, ±100 %) | fitted (aac6 loopback), assumed on the target |
| depth 1 / 2 | the general map's hidden fraction of its xGMI time: 0.011 at depth 1, 0.75 at depth 2; the equal path 0.75; memory: one more send + receive slab of one chunk of the top plane per APU | X13 (loopback, 2 and 3 node-processes) | measured (depth 1, equal path) / modelled (depth 2) |
| `NTT_MODMUL=1` | × 58.0 / 58.4 on the phases | K13 (one pair) | measured |
| mapping | init = the table's init + 0.065 s/GB × (mapped device − the table run's) | I (t_alloc), P3 | measured |
| the size-1 phases | `RUNS`: every current-code run with its phase split (1e9 … 1e11); the big products taken out of top / recip / div (the "rest" is cap-free), log-log interpolation in D | the logs on aac6 (paths in `mn_model.RUNS`) | measured |
| host at size 1 | 12.1 GB + 0.031 GB per 10⁹ digits above 4 × 10¹⁰ | the mem summaries of 4e10 / 8e10 / 1e11 | fitted |
| the 576-node piece | S13's C at 2³¹ over four APUs: 0.827 s at P = 3, 1.078 s at P = 4 (was 1.11) | S13 | measured |

What each strategy does at 576: rns_dist.c applies RNS_STRATEGY to dist_core, the single-node product. At 576 that is
each node's own top tree levels, since the distributed levels, the reciprocal and the division run mn_core. The model
therefore changes only the leaf's top levels and the planes' memory and mapping. That is agent B's own description in
rns_dist.c; it is assumed that the mn tier gets no B form.

## 2. Calibration (`./design_table.py --calibrate`; gate: wall within 3 %, peak within 1 %)

Size 1, series means (a series = the runs of one configuration; single runs scatter by their init, ±1.5 s):

| D | np | cap | code | n | measured | model | error | inputs? |
|---|---|---|---|---|---|---|---|---|
| 1e10 | 4 | 3·2³⁰ | 13 | 1 | 36.22 | 36.22 | 0.0 % | table |
| 4e10 | 4 | 3·2³⁰ | 13 | 8 | 81.49 | 81.81 | +0.4 % | table |
| 4e10 | 4 | 3·2³⁰ | 12 | 5 | 80.58 | 81.81 | +1.5 % | check |
| 4e10 | 4 | 2³¹ | 12 | 5 | 81.96 | 82.97 | +1.2 % | **check** (the cap axis at POOL_LOG 31: the per-product law alone) |
| 4e10 | 3 | 3·2³⁰ | 13 | 9 | 68.27 | 67.46 | −0.6 % | fit (the three-prime factors) |
| 4e10 | 3 | 2³⁰ (MM1) | 13b | 1 | 84.20 | 83.50 | −0.8 % | fit (cap_factor) |
| 4e10 | 3 | 3·2²⁹ (MM1) | 13b | 1 | 76.79 | 75.38 | −1.8 % | fit (cap_factor) |
| 8e10 | 4 | 2³¹ | 12 | 3 | 192.27 | 190.66 | −0.8 % | table (+ M11 v3 as a check, −2.5 %) |
| 1e11 | 4 | 2³¹ | 11 | 1 | 262.90 | 262.90 | 0.0 % | table |

Device totals: every run from 10⁹ to 10¹¹, at both prime counts and all four caps, within 0.05 %. Measured and
modelled examples: 4e10 at three primes 287.5 / 287.5; 2³⁰ 184.9 / 184.9; 3·2²⁹ 202.1 / 202.0; 1e11 431.2 / 431.2.
Node peaks (device + host HWM) from 4 × 10¹⁰ up: within 0.14 %.

Leave-one-out (each table size modelled without its own runs; a predictive check, not gated): 1e10 −1.8 %, 4e10
+11.1 %, 8e10 +3.3 %, 1e11 −4.1 %, 1e9 +31 %. The table points are 4–8 × apart in D, and the log-log interpolation
between them misses by up to 11 %. The M-run's sizes are table points (4 × 10¹⁰) and do not depend on it. The 576-node
leaf (5–10 × 10¹⁰ per node) sits between the 4e10, 8e10 and 1e11 points.

Exceptions (printed, not gated):
- 10⁹ at size 1. The wall there is 75 % init (±1.5 s a run), and the batch tier is latency-bound, so it does not follow
  the prime factor: −7.0 … +5.9 % per single run. The series 3·2³⁰ at three primes is +0.2 %. The model's wall covers
  10¹⁰ to 10¹¹.
- The node peak below 2 × 10¹⁰. The dm phase's host flows (X and R on the host, 26–40 GB at 10¹⁰) are not modelled;
  the device is exact. No ceiling is decided there.
- Single runs outside 3 % of their series, by their init: close10 run4 (init 23.0 s against the series' 21.3), P3 b6
  (18.6), and P3 b6 lmin8 (another switch, init 24.5).
- The loopback runs (g node-processes on one node over TCP). The same configuration spreads ±10 % there (M13: 101–125 s
  at 1e10/4), so they are gated at 10 %. 1e10/4 is −3.4 % and −1.4 %; 1e10/2 −3.9 %; the chunked 1e10/4 runs −5.3 %
  (shift) and −9.2 % (both, the T_ROUND fit's worst point). The forced-grid runs (`MN_TREE_LOGN_TEST`, M13 b2) are
  listed as not modelled. Memory per process at 1e10/4: device +2.5 %, because the exchange is counted on top of the dm
  need (conservative, M13); host 21.6 against 24.8 GB, the TCP copies.

## 3. `design_table.py`

- The rows: `mn_model.STRATEGIES × CAPS × CHUNKS × depth` = 96, each at `ECALC_NP=3 NTT_MODMUL=1`.
- Per row it gives (a) and (b), size 1 at 4 × 10¹⁰; (c) `max_digits(576, 502 / 480, design)`; (d) the run at the
  502-GB maximum; (e) the run at 4 × 10¹³ / 576 per node; (f) (e) at the low and high bandwidths (`--bws`, default
  50,100,200).
- The Pareto front is taken on ((e), (c) at 502) over the rows that hold 4 × 10¹³. F is the minimum (e); L is the
  maximum (c); R is the front row that maximises (its safe digits / L's) + (F's wall / its wall).
- Measured replacement (`--mrun`):
  - A (strategy, cap) with size-1 4 × 10¹⁰ runs at three primes and `NTT_MODMUL=1` gets its (a) and (b) measured.
  - Its 576-node leaf is scaled by measured / modelled (`leaf_scale`), and its ceilings use the budget less any
    measured peak excess.
  - Runs at one size and g that differ only in chunking refit `T_ROUND`.
  - A depth pair at a power-of-two size is reported as not exercising the switch.
  - A line with DIFFERS / FAILED is reported and not used.
- Options: `--lat`, `--write-bw`, `--hide-pow2`, `--gen-hide2`, `--e0` (a further t_strategy log), `--quick` (12 rows),
  `--make-line LOG KEY=V ...`.
- Tested:
  - `--calibrate` on the built-in runs: PASS.
  - A synthetic M-run log made from real runs (P3 b4 / b6, I, M13 b1 lines, one marked DIFFERS). The chunk lines refit
    T_ROUND to 0.082 s. The DIFFERS line is excluded. Two `ECALC_PLANE_CAP=2^31` lines at 68.5 s mean became a measured
    row, whose 576-node cells were scaled by 1.017 and whose ceiling dropped by the 13 GB peak excess.
  - `--make-line` on P3's 4 × 10¹⁰ log.
  - `--bws 40,80,160`.
  - `mn_model.py --calib` (legacy) unchanged: worst 6.1 %.
  - `estimate.py --legacy --max` unchanged: 6.95 × 10¹⁰, 4.3 min.

## Follow-up (integrator's request): X13b's depth measurements and B13b's strategy runs

**Depth 2 (X13b).**
- **Memory:** `mem_model` now charges one more chunk of the top plane (a quarter-plane, q/4) per APU on the general-map
  levels. That follows X's layout: 3 S → 4 S, measured +33 % (12.6 → 16.8 MB at size 2, 8.5 → 11.2 MB at size 3). My
  earlier guess was twice that.
- **Overlap:** the general map's hidden fraction at depth 2 is 0.74. It was 0.75 assumed; X measured 74.3 % on two real
  nodes (job 21068) and 72.6–75.1 % on one. Depth 1 stays 0.011 (1.4 % on real nodes).
- **Compute at 10⁹ on loopback:** X measured −7.4 % of the phases at size 3 and −9.9 % at size 4 with `DIST_GEN=1`.
  The model does **not** reproduce this: it gives −0.1 %. It is printed, not gated. The model hides the xGMI *link*
  time, a few ms per exchange on loopback. Depth 2 there also hides the host-side stage (pack, barrier, sync: 40–100×
  the link, X13 §3.1). The modelled target gain of depth 2 is therefore a lower bound.

**Strategies (B13b, 4 × 10¹⁰ at size 1, one run each on s24-30).**
- **auto:** `auto` uses the B form (`RNS_STRATEGY_FORM` default B), not B4. Its grid (`RNS_STRATEGY_GRID=1`) weighs the
  pieces that fit B at 0.70 per point and 3·2ᵏ lengths at 1.05. The model ports that grid (`_split_auto`), so under
  auto every product runs B.
- **B and B4 extra planes:** they are mapped inside bs, at the rates B measured: 11.47 s for 154.6 GB and 4.50 s for
  90.2 GB.
- **Calibration rows** (`--calibrate`, "the strategy rows"):

| form | phases measured | model | against the same node's C (measured / model) | device measured / model |
|---|---|---|---|---|
| auto (grid on) | 43.6 | 41.4 (−5.0 %) | −4.5 / −5.1 s | 287.5 / 287.5 |
| B4 | 46.7 | 46.8 (+0.2 %) | −1.7 / +0.2 s (4.0 % of C: listed as an exception) | 377.7 / 377.7 |
| B | 51.9 | 51.5 (−0.8 %) | +3.5 / +4.9 s | 442.1 / 442.1 |

- **Reading these rows:** the devices are exact. B's net-of-mapping estimates (B 40.4 s, B4 42.2 s) match the model
  (40.0 and 42.3). The misses come from the C baseline: these runs were on s24-30, whose C phases run 3–5 % above
  s24-26's (48.1–48.4 against 47.0 in the three-prime series; M13's four-prime 4 × 10¹⁰ 61.7 against 58.5).
- **Same node bias in cap_factor:** P's cap runs, which fit `cap_factor`, are on s24-30 too. The factor may therefore
  carry that node's +3–5 %; the M-run on one node settles it.

**Changed rows after the follow-up:**
- F (C, 2³¹, off, depth 2): 4.11 → **4.16** × 10¹³ (smaller depth-2 scratch); 4 × 10¹³ in 3.63 min, unchanged.
- R (C, 2³¹, both, depth 2): 5.47 → **5.52** × 10¹³; 4 × 10¹³ in 3.87 min, unchanged; 6.22 min at its maximum.
- L (B4, 2³⁰, both, depth 1): unchanged, 6.40 × 10¹³.
- auto rows: now 1.3 s faster than C at size 1 at 2³¹ (66.3 s against 67.6), and 62.0 s at 3·2³⁰ (the code's rule at
  4 × 10¹⁰), the best one-node row. At 576 auto equals C to within 0.01 min: only the leaf's top levels change.
- The Pareto front has 16 rows; the fabric ranking is unchanged (Spearman 1.000).

## Follow-up 2 (integrator): P's ceilings, make-line on multi-process logs, `--regen`

- **Agent P's runs as calibration rows** (three primes, `NTT_MODMUL=1`):
  - At 4 × 10¹⁰: 2³¹ 70.73 s (s24-30) and 67.89 s (s24-16); 3·2³⁰ 65.09 s. The 2³¹ series is −2.4 % against the model,
    and the 3·2³⁰ run +3.1 %.
  - The five edge runs: 1e11 at 3·2³⁰ 206.9 s; 1.14e11 at 3·2³⁰ 228.7; 1.30e11 at 2³¹ 353.1; 1.42e11 at 2³⁰ 677.2;
    1.44e11 at 2³⁰ 1085.8.
  - Device within 0.01 % on every one, node peak (device + host HWM) within 0.24 %.
  - **Init near the edge is slower than the mapping rate says:** 34.4–47.1 s at 465–509 GB of device, against 28–31
    modelled. `EDGE_INIT` (+10 s, ramped in from 440 to 465 GB) is fitted on those five runs, ±7 s.
  - With that term the 1e11 three-prime run is −0.5 % (gated). The walls beyond 1e11 are extrapolated and printed, not
    gated: 1.14e11 +3.2 %, 1.30e11 −4.5 %, 1.42e11 −7.7 %. The 1.44e11 run at 2³⁰ took 1085.8 s against 677 s for 1.42e11
    on another node, so the model's −41 % there is that run's anomaly.
- **The one-node ceiling at a 524-GB budget** (`--calibrate`, "the one-node ceiling"): the model gives 1.44 / 1.39 /
  1.30 / 1.14 × 10¹¹ at 2³⁰ / 3·2²⁹ / 2³¹ / 3·2³⁰. Every value lies between P's largest run that passed and smallest that
  failed (1.44–1.46, 1.40–1.42, 1.30–1.34, 1.14–1.16).
- **Table column (c) at 524 GB, "aac6 edge":** the 576-node digits at the aac6-measured budget, for example
  4.41 × 10¹³ for F, 5.88 for R and 6.76 for L. The table text says to size the target by 502 / 480 until its own edge
  is measured.
- **`--make-line`:**
  - On multi-process logs it reads the `mem[<rank>] [<phase>] device X GB in use ... peak live Y` lines. It writes the
    per-process device at init, the maximum in use over phases and ranks, the peak live, and the process count ("N
    processes on one node").
  - The verdict comes from an explicit `cmp=<verdict>` first. Otherwise only the exact words DIFFERS / "VERIFY FAILED"
    count; the earlier regex also matched "differ" in the verbose text of an identical run.
  - The M-run parser takes the line's last word as the verdict (identical / VERIFY / DIFFERS / *VERIFY-FAILED*), so the
    old lines with "DIFFERS identical" read as identical.
- **`--regen <logdir> [progress.txt]`:** rebuilds the M-run log from the tagged logs, one line per log with `tag=<tag>`.
  The tags are `s1_<strategy>_<cap>_r<n>` (K on), `koff_auto_2_31_r<n>` (K off), `series_auto_2_31_r<n>`,
  `p4_d<depth>_<off|shift|both>_r<n>` (size 4, `DIST_GEN=1 POOL_LOG=29`), and `p3_d<depth>_r<n>` (size 3,
  `POOL_LOG=29`). The verdicts come from `progress.txt`, passed as `cmp=`. A log that has no `total` line yet is skipped
  with a note. Tested on the five logs the campaign had written by then.
- **Agent K's kernels (`NTT_B1R=3 NTT_PLAN=1`) in the M-run:**
  - The model has no K term. The M-run's measured factor (koff against series / s1 at auto 2³¹) scales the rows the
    M-run does not measure (label `modelled x K`). The measured rows' own leaf scale already contains K.
  - Until K-off rows are in the log, the K-on rows are printed in `--calibrate` and not gated.
- **Chunking at 10¹⁰ / 4 with 1024-MB chunks:** the chunk is larger than the share, so there is no extra round and
  T_ROUND cannot be refitted from those rows. The note says so.
- **The table after this update:**
  - F, R and L are the same rows, with the same digits at 502 / 480 GB.
  - The walls at the 502-GB maximum rose with `EDGE_INIT` (R 6.22 → 6.37 min, L 12.08 → 12.24, F 3.70 min).
  - The walls at 4 × 10¹³ are unchanged, because the device stays below 440 GB there.

## M-RUN SWITCHES

D has no runtime switch (no C). What the M-run should give the table:

1. **One line per run** in the format `design_table.py` reads (`--make-line` writes it from a log):
   ```
   digits=40000000000 size=1 RNS_STRATEGY=<C|B|B4|auto> ECALC_PLANE_CAP=<2^30|3*2^29|2^31|3*2^30> [MDB_SHIFT_CHUNK_MB=.. MN_T_CHUNK_MB=.. COMM_ALLTOALLV_DEPTH=.. DIST_GEN=1] | <ecalc's total line> | device <mem summary's init device total> GB identical|DIFFERS
   ```
   One way is `./design_table.py --make-line <log> size=1 RNS_STRATEGY=B ECALC_PLANE_CAP=2^31 >> mrun.log`. ECALC_NP is
   read from the log (rns_init prints "three primes"); NTT_MODMUL is taken as 1 unless given. Run the 4 × 10¹⁰ runs with
   `MEM_REPORT_DEVS=1` or plain: the `mem summary` is printed either way.
2. **The size-1 block** (strategy × cap, three runs each): these become columns (a) and (b) and scale the 576-node leaf.
   A row the M-run does not cover stays modelled (for example B at 3·2³⁰, 450 GB of planes).
3. **The 10¹⁰ over 4 processes block**: chunking {off, shift, both} at one chunk size refits T_ROUND. For **depth**, 4
   node-processes are a power of two and never use the general map. Give both depth rows `DIST_GEN=1` (as X13b says), or
   run 3 processes; otherwise the depth pair is reported as uninformative. The depth's hidden fraction is better taken
   from X's `COMM_LAYER_STATS` both-busy numbers (`--gen-hide2`).
4. Then `./design_table.py --calibrate --mrun mrun.log` (gate) and `./design_table.py --mrun mrun.log` (the table).

## Open issues

- **cap_factor.** The pipeline's big products at POOL_LOG 30 run 1.30–1.39 × the per-product law, and the mechanism is
  not identified. It is fitted on one run per cap. At POOL_LOG 31 the law alone matches (I's 2³¹ runs, ≤ 0.3 s per
  phase). The candidates: the mdev tier's pieces at 3·2²⁸ with three primes, pool 1 at the batch tile, and
  `DIST_LOGN_TEST=30` set by `ECALC_PLANE_CAP`. Whether the same factor applies to the mn tier's pieces at 576 is not
  known; the model applies it only to the leaf's single-node products.
- **Three primes at 8 × 10¹⁰ and 10¹¹ are not measured.** The factor comes from the 4 × 10¹⁰ series. P's 10¹¹ edge runs,
  or the M-run, would check it. The four-prime 10¹¹ point is Phase 11 code.
- **T_ROUND on the target.** It is fitted on loopback TCP at ±100 %, and it decides how much the chunked rows cost (§6
  item 4 of TARGET.md).
- **Depth 2.** The hidden fraction (0.75) is modelled. X13b's both-busy measurement replaces it (`--gen-hide2`).
- **The leave-one-out error of the size-1 phase table** is up to 11 % between table points. That matters only for D
  between the measured sizes. The 576 leaf at 5–10 × 10¹⁰ is bracketed by the 4e10, 8e10 and 1e11 points.
- **Host at size > 1** stays the Phase 12 constants (6 GB per process + the SHMEM pool; M13 open issue 1).
- **The B forms' extra planes are charged to init** at the mapping rate. In the code they are a hipMalloc in the phase,
  the same cost at another time.

## RESUME

- Done: models (np, strategy, cap, chunking, depth, modmul), `design_table.py` (table, Pareto, sensitivity, M-run
  ingestion, calibration, make-line), TARGET.md §1/§3/§6, the table in `results/DESIGN_TABLE.md` (all modelled),
  calibration gate PASS.
- Inputs taken from this session's agents: B's `t_strategy` (job 21047) and P's cap runs (job 21046, the 2³⁰ and
  3·2²⁹ 4 × 10¹⁰ runs; P's 2³¹ and 3·2³⁰ runs were still running when this was written: add them to `mn_model.RUNS` as
  `use='check'` rows).
- Next, for the integrator at hour 4.5: `cd ecalc && ./design_table.py --calibrate --mrun <log> && ./design_table.py
  --mrun <log>`, then copy the marked rows into RESULTS §79.
