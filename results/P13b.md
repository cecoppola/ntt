# P13b — sizing follows the prime count; the plane cap as one switch; the one-node ceiling at three primes (Phase 13b, PLAN §31 row P)

Branch `p13b-P` (from `p13b-step0` @ c988218). aac6 clone `~/ntt-P13b`, logs `~/ntt-P13b/ecalc/results/p13b/b*/`,
batch scripts `~/p13b_b*.sh`. Labels: **measured** (aac6, s24-26), **modelled** (the C layout report / mem_model),
**assumed**.

## M-RUN SWITCHES

One switch, `ECALC_PLANE_CAP`, off by default (unset: POOL_LOG and the 3·2ᵏ size rule exactly as on step 0). Each value
is exactly the three knobs on its row; the switch sets them and refuses a conflicting value already in the environment.

| cap (points per product plane) | exact env | = the knobs it sets | plane pools per node at P = 3 (modelled = C formula) |
|---|---|---|---|
| 2³⁰ | `ECALC_PLANE_CAP=2^30` | `POOL_LOG=30 RNS_PLANES_3Q30=0 DIST_LOGN_TEST=30` | 60.1 GB (+0.61 tables) |
| 3·2²⁹ | `ECALC_PLANE_CAP=3*2^29` | `POOL_LOG=30 RNS_PLANES_3Q30=1 DIST_LOGN_TEST=30` | 77.3 GB |
| 2³¹ | `ECALC_PLANE_CAP=2^31` | `POOL_LOG=31 RNS_PLANES_3Q30=0 DIST_LOGN_TEST=31` | 103.1 GB |
| 3·2³⁰ | `ECALC_PLANE_CAP=3*2^30` | `POOL_LOG=31 RNS_PLANES_3Q30=1 DIST_LOGN_TEST=31` | 154.6 GB |
| (fit) | `ECALC_PLANE_CAP=fit [ECALC_NODE_GB=480]` | the largest of the four whose node total fits the budget | — |

Aliases accepted: `30`, `3x29`, `31`, `3x30` (quote `3*2^29` in shell). The default today (switch unset) is 3·2³⁰ below
5 × 10¹⁰ digits and 2³¹ from 5 × 10¹⁰ (the Phase 12 I size rule, unchanged).

(results below: filled in by batch)

## RESUME

* Code: commit d1bf03b (switch, formula, layout report). Batch 1 (job 21046) queued behind the integrator's S0.
