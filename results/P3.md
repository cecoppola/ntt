# P3 — three primes for the decimal pipeline (Phase 13a, PLAN §29 E1 + E3)

Branch `p13-P3`. **The EC_NP parameterisation commit for agent S: `32101f8`** (first working build: `03d3e82`,
which adds only the t_crt generalisation; S should rebuild on the branch head).

Switch: `ECALC_NP=3|4` (default 4 — every default unchanged). `ECALC_NP=3` needs decimal limbs (the pipeline's
default); with `LIMB_BASE=2` it is refused at `rns_init` with a message (exit 1).

## Design

* **Prime subset**: the first three primes of the set, c = 240, 216, 207 (p_i = c 2^44 + 1). tests/t_primes prints all
  four subsets: this one has the largest product, 2^155.355, and so the largest margin; all four share the kernels
  (3 | c: 2^k and 3·2^k lengths, p < 2^52). Using primes 0..2 means no reordering anywhere: "prime p on device p"
  and "plane p" stay as they are.
* **The bound** (measured by exact GMP arithmetic in t_primes): a convolution coefficient is at most
  nterms·(B−1)², B = 10^18, so three primes reconstruct exactly for nterms ≤ 58 424 467 928 (2^35.766). Margin
  p0p1p2 / (n (B−1)²): 27.2x at 2^31, 13.6x at 2^32, **6.80x at 2^33**, 2.27x at 3·2^33 (the longest length the primes
  allow). Every product the primes can transform is inside the bound. Binary 2^64 limbs would allow only 2^27.36 terms,
  so four primes stay required there.
* **`ec_np`** (modarith.h, defined in crt.c): runtime prime count, read once from ECALC_NP by `ec_np_init()` (called
  by `crt_init`, `rns_init`, `rns_gconst`). `EC_NP` stays 4 as the array bound and the device count.
  `ec_np_check(nterms, decimal, where)`: aborts loudly if ec_np = 3 and the limbs are binary or nterms exceeds the
  bound; called by `rns_init`, `crt_carry_par4(_q)`, `rns_mul_mdev`, `rns_mul_batch`, `dist_core` and `mn_core`.
* **Three-prime CRT**: Garner's first two steps (x = r0 + t1 p0 + t2 p0 p1 < 2^156, 3 words) — `garner3` (CPU, crt.c)
  and `dgarner3` (GPU, rns_mul.c) — then `ec_words_to_dec3` (modarith.h): 3 divisions by 10^18 instead of dec4's 16,
  because the top word is < 2^28 and the second quotient < 2^37. The carry window is 3 digits + carry; the spill format
  stays 4 words (the last 0), so every spill merge (CPU, `db_add_spills`, the dist tiers) is unchanged.
  `k_crt_batch` takes the prime count from `struct gconst` (`g.np`), so its signature and every call site are unchanged.
  Measured, CPU CRT 2^26 decimal coefficients at T = 192: 4 primes 0.377 s, **3 primes 0.175 s (2.15x)**.
* **Tiers**:
  - batch-local (every prime of a product on its own APU): loops and plane counts over `ec_np`; `k_scatter4` leaves
    plane 3 unwritten.
  - mdev and the striped batch tier (prime d on device d): device 3 skips its transform with three primes and joins
    only the GPU CRT (which is striped over all four devices). Not load-balanced — see open issues.
  - dist (`dist_core`, `mn_core`, the transform cache's slot size): prime loops and plane sizing `ec_np · q`.
* **Plane pools**: with ec_np = 3, plane pool 0 is allocated at init to exactly 3 q (3/4 of the four-prime pool; q =
  2^(pool_log−2), or 3·2^(pool_log−3) with the B3 planes). Pool 1 (xb | sbuf | rbuf, one prime at a time) is
  unchanged at 3 q + 16 — so the planes drop by 1/7, not 1/4: **modelled 180.4 → 154.6 GB per node** at the 4e10
  configuration (pool 0 25.8 → 19.3 GB per APU, pool 1 19.3 GB), not the 135.3 of PLAN §29 E3 (which scaled both
  pools). The mdev tier's largest product is then capped by pool 0 (`mdev_pts()`: 3·2^(pool_log−2) points with the
  3·2^k lengths; four primes: 2^pool_log, unchanged).

## 4 × 10¹⁰ on one node: three primes against the default (measured)

Job 21009, node s24-26, commit 03d3e82, one batch, the reference evicted from the page cache before each run, the
digits cmp'd after each (plus an earlier ECALC_NP=3 run in job 21005 on the same node: 66.40 s, phases 46.5, identical).

| | default (ECALC_NP=4) | ECALC_NP=3 | change |
|---|---|---|---|
| wall (`total`) | 81.30 s | **66.51 s** | −14.8 s (−18 %) |
| phases (bs + 10dP + dm) | 58.8 s | **46.9 s** | −11.9 s (−20 %) |
| bs (batch / top levels) | 32.6 s (22.1 / 10.5) | 25.9 s (17.5 / 8.3) | −6.7 s |
| dm (reciprocal) | 26.1 s (12.3) | 20.9 s (9.8) | −5.2 s |
| init | 22.5 s | 19.6 s | −2.9 s (26 GB less of plane pools mapped) |
| device memory in use, every phase (peak) | 313.3 GB | **287.5 GB** | −25.8 GB |
| of it: plane pools | 180.4 GB | 154.6 GB | −25.8 GB (pool 0 at 3 q) |
| of it: region / block pool, tables | 132.3 + 0.61 GB | 132.3 + 0.61 GB | unchanged |
| peak host (VmHWM) | 12.2 GB | 12.1 GB | |
| digits vs results/e_4e10.out | identical | identical | |

Single runs, not a five-run series (PLAN §29.6 asks for five; the closing series of Phase 12 measured the default at
80.7 ± 1.2 s, so the 14.8 s gap is ~12 standard deviations). The block pool is sized independently of the plane pools
(M's `tree_need_dev` / arena sizing), so the 25.8 GB freed per node is not yet turned into a higher digit ceiling: it
is free device memory. The expected transform saving (−25 %) shows up as −20 % of the phases; the CRT kernels also
become cheaper (3 divisions by 10^18 per coefficient instead of 16).

## Files touched

Owned: `modarith.h`, `crt.c`, `crt.h`, `rns_mul.c`, `rns_int.h` (one field in `struct gconst`), `tests/t_crt.c`,
new `tests/t_primes.c`. In `rns_dist.c` only the prime-count / plane-size lines (PLAN §30 correction 2): the prime
loops and `ec_np · q` sizing in `dist_core` and `mn_core`, `cache_slot_bytes`, `xa[] = {0}`, and one `ec_np_check`
line in each of `dist_core` / `mn_core`. `ntt_dist.c`: nothing (its passes are per prime; the loops live in
rns_dist.c). **Outside my files**: `Makefile` — the `tests/t_primes` target and its entry in `TESTS` (2 lines).

## Tests (all measured on aac6, job 21005, node s24-26, commit 03d3e82)

| test | result |
|---|---|
| `./tests/t_primes` (parts 1–3, CPU) | VERIFY OK (1 000 083 checks): bound table, 19 extreme + 10^6 random Garner values vs GMP, CRT with every coefficient at 2^33 (B−1)², at the largest allowed and at p0p1p2−1 (T = 1, 7, 96, 192), the exact all-(B−1) profile at 2^22 (and == the four-prime CRT) |
| `ECALC_NP=3 ./tests/t_crt 30` | VERIFY OK (12 checks; decimal; part 3 binary-only, skipped) |
| `./tests/t_crt 30` (default) | VERIFY OK (24 checks) |
| `ECALC_NP=3 ./tests/t_primes gpu 30` | VERIFY OK (87 checks): all-(B−1) n×n (every coefficient at its maximum) exact through rns_mul up to 2^29×2^29 and **rns_mul_dist up to 2^30×2^30 (2^31 points, peak coefficient 2^30 (B−1)²)**; random operands vs mpz_mul / mod 2^61−1; the striped batch tier (16 products at 2^10, 2^14, 2^18) |
| `ECALC_NP=3 LIMB_BASE=2 ./ecalc 1000000` | refused, exit 1: "rns_init: ECALC_NP=3 needs base-10^18 limbs ..." |
| `ECALC_NP=3 LIMB_BASE=10 ./tests/t_mul 20` | VERIFY OK (191 checks), 29.5 min (the decimal GMP conversions) |
| `ECALC_NP=3 ./ecalc 1000000000` | VERIFY OK, **cmp identical** to ref/e_1000000000.txt; total 15.09 s (phases 3.4 s, init 11.4 s), VmHWM 15.7 GB |
| `./ecalc 1000000000` (default, same node, next run) | VERIFY OK, identical; total 18.21 s (phases 3.9 s, init 13.9 s) |

## RESUME

* Done: code (commits 32101f8, 03d3e82), the tests above.
* 10^9: identical to ref at ECALC_NP=3 (total 15.1 s, VmHWM 15.7 GB) and at the default (18.2 s) — batch b2.
* 4e10: ECALC_NP=3 66.5 s against the default's 81.3 s, same node and batch, both identical (table above; batches
  b3, b4).
* Running: mnaccept --only unit,e9 in job 21009 (results/P3/b4.out, results/mnaccept/21009/ on aac6).
* Next: mnaccept `--only unit,e9` on the branch; a 4e10 run at ECALC_NP=3 vs default (wall, peak device memory,
  cmp against results/e_4e10.out).
* aac6 clone ~/ntt-P313 (branch p13-P3), logs in ~/ntt-P313/ecalc/results/P3/.
