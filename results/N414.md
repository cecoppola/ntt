# N414 — `t_mn_grid` on real nodes over SOS, `mnrun.sh`'s SHMEM detection, the dist tier's load speed (Phase 14, PLAN §33 A4, A5)

Branch `p14-N4` (from `main` @ a839418). aac6 clones: `~/ntt-N414` (plain build, A5) and `~/ntt-N414-sos`
(`make SHMEM_HOME=~/sos`, A4), both at the branch head. Scripts and logs on aac6 in `~/N414/` (`a4.sh`, `a5.sh`,
`props.sh`, `thptouch.c`; `a4/`, `a5/<batch>/`, `a5/summary.txt`).

## RESUME

- A4(b) code done (commit 278daf9): `mnrun.sh` detection through wrappers, `COMM_SHMEM_IMPL` validated,
  `MNRUN_SHOW_IMPL` test hook; README rows. Detection verified on aac6 (job 21267, see §1).
- A5 batch b1 (s24-16) queued at 09:03 CDT behind P2's jobs: `~/N414/a5_b1.out`.
- A4(a) batch not yet run (`~/N414/a4.sh`, -N3; relaunch with `setsid nohup bash ~/N414/a4.sh > ~/N414/a4.out 2>&1 &`).
