X READY FOR 3-NODE SLOT

# X13 — xGMI and the fabric at the same time (PLAN.md §29 E9 part 1, H4), Phase 13a

Branch `p13-X` (from `main` @ 203733b). Files: `ecalc/comm_layered.c`, `ecalc/comm_xgmi.c`, `ecalc/tests/t_dist.c`
(nothing else touched). aac6 clone `~/ntt-X13`; batches `~/x13/b*.sh`, logs `~/x13/b*/`.

(work in progress — see RESUME at the end)

## RESUME

* Done: the instrumentation (COMM_LAYER_STATS=1|2, COMM_XGMI_STATS=1), the drivers (`DIST_H4=1`, `DIST_LBENCH=` under
  `DIST_LAYERED=1`), H4 on one node (job 21006, `~/x13/b1/h4_*.txt`), loopback sanity (`~/x13/b1/`, `~/x13/b1b/`).
* Next: batch 2 on two real nodes (`~/x13/b2.sh <job>`, -N2), then the 3-node slot (`~/x13/b3.sh`), then
  `mnaccept.sh $J --only unit,e9,mn` and a 4e10 run with COMM_XGMI_STATS=1 (H4 in the pipeline).
