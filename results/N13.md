# Phase 13a agent N — checkpoint hardening (TASKS 1.7, 4.1, 1.4)

Branch `p13-N` (base `203733b`). Draft; numbers below are filled in as the batches run.

## RESUME

- Done: code for all three items (binsplit.c checkpoint functions, mn.c tree-set write, ecalc.c output tail; minimal hooks in
  newton_db.c/newton.h and mn_out.c), `ecalc/n13_ckpt_test.sh`. Built on aac6 in `~/ntt-N13` (`ecalc.base` = the base commit's binary).
- Next: batch 1 `./n13_ckpt_test.sh $J --only restart,bg,budget`; batch 2 `--only time`; batch 3 `./mnaccept.sh $J --only unit,e9,ckpt,recheck`.
- Live: none.
