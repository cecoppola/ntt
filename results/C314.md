# C3 (Phase 14): A3 clean failure from worker threads; E12 the collective budget check

Branch `p14-C3` from `int14c` @ 9ad8fd4. Delivery to aac6 by a bundle from 4bc48ee (clone `~/ntt-C314`).
Note: the common instructions file (`agent_common14.txt`) was not present in the scratchpad; the protocol followed is the
one of the sibling results files (R114, S114): commit + RESUME after every step, node work as unattended self-cancelling
batches (<= 45 min, one job at a time).

## RESUME (updated step 1)

- **Done**: step 1 (A3 code): `ecalc/fatal.c/.h` (`ec_fatal`, `ec_fatal_begin/_end`, `ec_quit`, `ec_fatal_phase`); every
  `exit(1)` / `abort()` of `ecalc/*.c` (except the main thread's usage errors, `exit(2)`, `mn_plan.c` and the collective
  BS_RESTART refusal `exit(4/5)` in `mn.c`) routed through it (176 single-line sites by a script, 11 by hand: the two
  in-phase pool guards, `mem_oom`, `bigint` allocation failures, `crt.c` np checks, `comm_tcp die`, the ntt switch
  defaults, the bs checkpoint refusal); `ECALC_FATAL_TEST=1` (the four bs threads fail together); Makefile (`fatal.o`).
  Not yet built with hipcc (no hipcc on this machine; aac6 login node next).
- **Next**: step 2 (E12 code: `ECALC_BUDGET_CHECK=1` in `ecalc.c` after init), then deliver, build, node batch.
