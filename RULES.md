(1) Be very token-efficient in your work. Find ways to use fewer tokens when searching, reading, thinking, writing, and testing. Always priorities using CPU time and memory over tokens. If it will save you tokens, run more commands and execute harder tasks. Do whatever you can to minimize the total number of tokens you use to complete any task.

(2) Always make sure that code comments accurately reflect what the current version of the code is actually doing. If code changes, you must change the comments. If you review files, check the comments for consistency. Always ensure the comments are accuratre.

(3) Always make sure the project files reflect what the current version of the code is doing. After development, make sure the project files are up to date. Any time you write to a project file, ensure that you are accurately representing the current state of the project.

(4) Write very neat, elegant, compact print statements to the terminal when you are coding. Output all of the important information about what the code is doing in a nicely formatted way. I need to be able to see exactly what the code is doing while you are working and testing.

(5) Rely on testing, benchmarking, and diagnostics. Create files to store this information. Run tests over a range of variable values and evaluate their results. Feel free to use greater amounts of compute time, while using very few tokens, to collect information that will lead to code improvements. Feel free to construct neat, clear tables in files for this purpose.